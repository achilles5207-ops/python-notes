# 🛠️ Scripts

This directory contains standalone Python utility scripts for the project.

- **`generate_notebooks.py`**: A script that automates the generation of the interactive Jupyter practice notebooks (`.ipynb` files) using the `nbformat` library.
- *(Add more scripts here as needed)*
